# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Real family names/emails and login passwords — local dev only, never committed
server/db/seed.ts

# Internal planning docs (specs/plans/briefs/reports/reviews) — local only
docs/

# Real uploaded photos — never commit, only the tracked placeholder-*.svg fallbacks
public/uploads/*
!public/uploads/placeholder-*.svg
